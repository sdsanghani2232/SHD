<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SHD" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimaryDark">@color/app_color</item>
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="Theme.SHD" parent="Base.Theme.SHD" />

    <style name="Theme.SHD.Button" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/login_button_bg</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>