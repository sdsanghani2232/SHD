<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/select_icon"
        android:icon="@drawable/not_select"
        android:iconTint="@color/search_icon_color"
        android:contentDescription="@string/select"
        android:title="@string/select"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />

    <item android:id="@+id/download"
        android:icon="@drawable/download_icon"
        android:iconTint="@color/search_icon_color"
        android:contentDescription="@string/download"
        android:title="@string/download"
        app:showAsAction="always"
    />

    <item android:id="@+id/share_menu"
        android:icon="@drawable/share_icon"
        android:iconTint="@color/search_icon_color"
        android:contentDescription="@string/share"
        android:title="@string/share"
        app:showAsAction="always"
        />

    <item android:id="@+id/more_menu"
        android:icon="@drawable/more_menu"
        android:iconTint="@color/search_icon_color"
        android:contentDescription="@string/more_menu"
        android:title="@string/more_menu"
        app:showAsAction="always"
        />



</menu>